const _kHz = x => x * 1000;
const _MHz = x => x * 1000 * 1000;
const _GHz = x => x * 1000 * 1000 * 1000;

const _kBit = x => x * 1024;
const _MBit = x => x * 1024 * 1024;
const _GBit = x => x * 1024 * 1024 * 1024;

const _kB = x => x * 1024;
const _MB = x => x * 1024 * 1024;
const _GB = x => x * 1024 * 1024 * 1024;